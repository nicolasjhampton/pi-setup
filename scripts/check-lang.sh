#!/bin/bash

git --version
g++-8 --version
clang-6.0 --version
java --version     
javac --version
python --version
python3 --version
pipenv --version
rbenv --version     
rbenv versions   
echo "$(command -v nvm)"
node --version
go version
rustup --version
rustc --version
cargo --version
dotnet --version
docker --version

exit 0
